import styled from 'react-emotion';
import {colors} from '../../../components/theme';

export const ScoreBoardContainer = styled.div({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  borderBottom: `1px solid ${colors.xxLightCoolGray}`,
});
